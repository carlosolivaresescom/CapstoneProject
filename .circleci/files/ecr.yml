Description: >
  Carlos / Udacity Project

Parameters:
  ID:
    Description: Unique identifier.
    Type: String

Resources:
  ECR:
    Type: AWS::ECR::Repository
    Properties:
      RepositoryName: "ecr-repository"

Outputs:
  ECR:
    Description: A reference to the created ECR
    Value: !GetAtt ECR.RepositoryUri
    Export:
      Name: !Sub "${ID}-ECR-Uri"
